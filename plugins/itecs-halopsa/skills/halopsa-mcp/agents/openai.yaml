interface:
  display_name: "ITECS HaloPSA MCP"
  short_description: "Use read-only HaloPSA tools for clients, billing, purchase orders, projects, and tickets."
  brand_color: "#2563EB"
  default_prompt: "Use the ITECS HaloPSA read-only MCP tools to look up purchase orders, projects, tickets, and related billing evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "halopsa"
      description: "Bundled ITECS HaloPSA read-only MCP server"
      transport: "stdio"
