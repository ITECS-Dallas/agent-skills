#!/usr/bin/env bash
set -euo pipefail
if command -v python3 >/dev/null 2>&1; then
  exec python3 "$(dirname "${BASH_SOURCE[0]}")/doctor.py" "$@"
fi
exec python "$(dirname "${BASH_SOURCE[0]}")/doctor.py" "$@"
